# ColorPicker

A cross-platform color picker tool built with Electron.

## Features
- Eyedropper for selecting colors from the screen.
- Preview selected colors in Hex and RGB formats.
- Slick and clean UI.
- Color blindness simulation and contrast checking.

## Installation
1. Clone the repository.
2. Run `npm install`.
3. Run `npm start`.
